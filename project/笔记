虚拟列表：
只渲染可视区DOM节点，避免直接渲染所有内容性能下降。
思路：
1.总高度：根据列表项高度（固定每一项高度）和数量计算并设置虚拟滚动条高度
2.可视区渲染数量
3.通过当前位置计算可视区渲染内容在数组中的起始位置
4.根据起始位置渲染数组内容
js实现：
1.设置每一项具体高度
2.计算 总高度 = 每一项高度 * 列表项数量
3.计算 一屏展示数 viewAccount= 可视区高度 / 每一项高度
    可视区高度container.clientHeight不含高度不含边框，offsetHeight含滚动条含边框(也是自带属性)
4.设置 渲染起始位置 startIndex = 0, endIndex = viewAccount 
5.渲染数组内容：从 startIndex 到 endIndex 的内容
————————————以上实现一屏静态展示——————————————————
6.监听滚动事件 实现滑动
  通过container自带属性scrollTop:元素内容顶部到视口顶部的距离
    6-1 重新计算渲染 起始位置
    6-2 渲染数组内容


